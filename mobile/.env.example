# --- Supabase Auth (same project as the web app) ---
EXPO_PUBLIC_SUPABASE_URL="https://xxxx.supabase.co"
EXPO_PUBLIC_SUPABASE_ANON_KEY="eyJ..."

# --- LeadRelay web API base URL ---
# Local dev: your machine's LAN IP so a device/emulator can reach `next dev`
# (Android emulator can use http://10.0.2.2:3000). Production: the deployed URL.
EXPO_PUBLIC_API_URL="http://192.168.1.100:3000"
